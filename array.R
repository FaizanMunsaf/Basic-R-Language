#Array are the R data objects which can store data in more than two dimensions.
#syntax -> array(data, dim, dimnames)

arr = array(c(1:9),dim = c(3,3,4,2)) # firstly we create 1 to 9 range number and first 3 and 2nd 3 represent 3x3 matrix and 4x2 array
# if our values is incomplete thenn it's start to add element from the start


