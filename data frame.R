#Data Frame is a table or a two-dimensional aray-like structure in which each column contains values of one variable and each row contains one set of values from each column.
#as well as we store data in a table directly through the data frame from data base
vtr1 = c(1:5)
vtr2 = c("Faizan", "Munsaf", "Ghania", "Iftikhar", "Sameer")
vtr3 = c(25,15,55,123,32)
data.frame(vtr1, vtr2, vtr3)

data.frame(airquality)