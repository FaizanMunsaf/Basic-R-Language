#lists are the R objects which contain elements of different types like - numbers, strings, vectors and another list inside it.
# list(data)

vtr1 = c(5.532,23,534,32.1)
vtr2 = c("Hey ", "How are You?", " Thank You")

mylist = list(vtr1,vtr2)